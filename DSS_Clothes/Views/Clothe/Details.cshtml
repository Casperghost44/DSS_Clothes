@model DSS_Clothes.Models.Clothe

@{
    ViewData["Title"] = "Details";
}

<h1>Details</h1>

<div>
    <h4>Clothe</h4>
    <hr />
    <dl class="row">
        <dt class = "col-sm-2">
            @Html.DisplayNameFor(model => model.ClotheName)
        </dt>
        <dd class = "col-sm-10">
            @Html.DisplayFor(model => model.ClotheName)
        </dd>
        <dt class = "col-sm-2">
            @Html.DisplayNameFor(model => model.Brand)
        </dt>
        <dd class = "col-sm-10">
            @Html.DisplayFor(model => model.Brand.BrandName)
        </dd>
        <dd class = "col-sm-10">
            <table class="table">
                <thead>
                    <tr>
                        <th>
                            Stores
                        </th>
                        
                </thead>
                <tbody>
                    @foreach (var item in Model.Designers)
                    {
                        <tr>
                            <td>
                                @Html.DisplayFor(modelItem => item.Stores.StoreName)
                            </td>
                            <td>
                                <a asp-controller="Designer" asp-action="Delete" asp-route-id="@item.DesignerID">Delete</a>
                            </td>
                        </tr>
                    }
                </tbody>
            </table>
        </dd>
    </dl>
</div>
<div>
    <a asp-action="Edit" asp-route-id="@Model?.ClotheID">Edit</a> |
    <a asp-action="Index">Back to List</a>
</div>
